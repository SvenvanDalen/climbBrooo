<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="24dp"
        android:paddingEnd="24dp"
        android:paddingTop="8dp">

        <EditText
            android:id="@+id/input_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Naam (bv. Racefiets)"
            android:importantForAutofill="no"
            android:inputType="textCapSentences" />

        <CheckBox
            android:id="@+id/check_archive"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Km uit rittenarchief meetellen" />

        <Button
            android:id="@+id/btn_since"
            style="@style/Widget.ClimbPro.Button.Outline"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp" />

        <CheckBox
            android:id="@+id/check_virtual"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Indoorritten (VirtualRide) meetellen" />

        <EditText
            android:id="@+id/input_extra_km"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Extra km handmatig (leeg = 0)"
            android:importantForAutofill="no"
            android:inputType="number" />

        <CheckBox
            android:id="@+id/check_retired"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Uit gebruik (nieuwe ritten tellen niet meer mee)" />
    </LinearLayout>
</ScrollView>
